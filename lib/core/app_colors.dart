import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary & Accent
  static const Color primary = Color(0xFF34A753);
  static const Color accent = Color(0xFFFF751F);

  // Background
  static const Color background = Color(0xFFF6F7FB);
  static const Color surface = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF1A1D26);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textTertiary = Color(0xFF9CA3AF);

  // Status tints — soft versions for badges
  static const Color primaryTint = Color(0xFFE8F5E9);
  static const Color accentTint = Color(0xFFFFF3E0);
  static const Color warningTint = Color(0xFFFFF8E1);
  static const Color dangerTint = Color(0xFFFFEBEE);

  // Card shadow
  static const Color cardShadow = Color(0x0A000000);

  // Divider
  static const Color divider = Color(0xFFE5E7EB);

  // Gauge track
  static const Color gaugeTrack = Color(0xFFEEF0F4);
}
